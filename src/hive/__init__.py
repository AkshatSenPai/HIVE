"""HIVE — autonomous business operating system.

P0 base: orchestration core (event bus, coordinator, job FSM), three-agent
roster, memory fabric skeleton, policy engine, governance surfaces, traces.
The business itself is mounted as an adapter (see adapters/).
"""

__version__ = "0.0.1"
